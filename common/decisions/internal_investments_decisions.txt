# Investor decisions (15 project slots, visible = no - activated via scripted effects).
internal_investments_DECISIONS = {
	internal_investments_project_0_decision = {
		icon = GFX_decision_category_generic_industry

		priority = 5

		highlight_states = {
			highlight_states_trigger = {
				check_variable = { ROOT.internal_project_array^0 = THIS.id }
			}
		}

		visible = { always = no }

		days_remove = project_construction_duration^0

		cancel_trigger = { NOT = { check_variable = { ROOT.internal_project_array^0 < 0 } } }

		remove_effect = {
			log = "[GetDateText]: [ROOT.GetName]: Decision investments_project_0_decision"
			set_temp_variable = { completed_project_building_type = project_building_type^0 }
			custom_effect_tooltip = {
				localization_key = internal_investment_completion_tooltip_tt
				BUILDING = [investments_get_completed_building_type]
				AMOUNT = [?ROOT.project_build_amount^0]
			}
			hidden_effect = {
				set_variable = { project = 0 }
				country_event = investments_event.0
			}
		}
	}
	internal_investments_project_1_decision = {
		icon = GFX_decision_category_generic_industry

		priority = 5

		highlight_states = {
			highlight_states_trigger = {
				check_variable = { ROOT.internal_project_array^1 = THIS.id }
			}
		}

		visible = { always = no }

		days_remove = project_construction_duration^1

		cancel_trigger = { NOT = { check_variable = { ROOT.internal_project_array^1 < 0 } } }

		remove_effect = {
			log = "[GetDateText]: [ROOT.GetName]: Decision investments_project_1_decision"
			set_temp_variable = { completed_project_building_type = project_building_type^1 }
			custom_effect_tooltip = {
				localization_key = internal_investment_completion_tooltip_tt
				BUILDING = [investments_get_completed_building_type]
				AMOUNT = [?ROOT.project_build_amount^1]
			}
			hidden_effect = {
				set_variable = { project = 1 }
				country_event = investments_event.0
			}
		}
	}
	internal_investments_project_2_decision = {
		icon = GFX_decision_category_generic_industry

		priority = 5

		highlight_states = {
			highlight_states_trigger = {
				check_variable = { ROOT.internal_project_array^2 = THIS.id }
			}
		}

		visible = { always = no }

		days_remove = project_construction_duration^2

		cancel_trigger = { NOT = { check_variable = { ROOT.internal_project_array^2 < 0 } } }

		remove_effect = {
			log = "[GetDateText]: [ROOT.GetName]: Decision investments_project_2_decision"
			set_temp_variable = { completed_project_building_type = project_building_type^2 }
			custom_effect_tooltip = {
				localization_key = internal_investment_completion_tooltip_tt
				BUILDING = [investments_get_completed_building_type]
				AMOUNT = [?ROOT.project_build_amount^2]
			}
			hidden_effect = {
				set_variable = { project = 2 }
				country_event = investments_event.0
			}
		}
	}
	internal_investments_project_3_decision = {
		icon = GFX_decision_category_generic_industry

		priority = 5

		highlight_states = {
			highlight_states_trigger = {
				check_variable = { ROOT.internal_project_array^3 = THIS.id }
			}
		}

		visible = { always = no }

		days_remove = project_construction_duration^3

		cancel_trigger = { NOT = { check_variable = { ROOT.internal_project_array^3 < 0 } } }

		remove_effect = {
			log = "[GetDateText]: [ROOT.GetName]: Decision investments_project_3_decision"
			set_temp_variable = { completed_project_building_type = project_building_type^3 }
			custom_effect_tooltip = {
				localization_key = internal_investment_completion_tooltip_tt
				BUILDING = [investments_get_completed_building_type]
				AMOUNT = [?ROOT.project_build_amount^3]
			}
			hidden_effect = {
				set_variable = { project = 3 }
				country_event = investments_event.0
			}
		}
	}
	internal_investments_project_4_decision = {
		icon = GFX_decision_category_generic_industry

		priority = 5

		highlight_states = {
			highlight_states_trigger = {
				check_variable = { ROOT.internal_project_array^4 = THIS.id }
			}
		}

		visible = { always = no }

		days_remove = project_construction_duration^4

		cancel_trigger = { NOT = { check_variable = { ROOT.internal_project_array^4 < 0 } } }

		remove_effect = {
			log = "[GetDateText]: [ROOT.GetName]: Decision investments_project_4_decision"
			set_temp_variable = { completed_project_building_type = project_building_type^4 }
			custom_effect_tooltip = {
				localization_key = internal_investment_completion_tooltip_tt
				BUILDING = [investments_get_completed_building_type]
				AMOUNT = [?ROOT.project_build_amount^4]
			}
			hidden_effect = {
				set_variable = { project = 4 }
				country_event = investments_event.0
			}
		}
	}
	internal_investments_project_5_decision = {
		icon = GFX_decision_category_generic_industry

		priority = 5

		highlight_states = {
			highlight_states_trigger = {
				check_variable = { ROOT.internal_project_array^5 = THIS.id }
			}
		}

		visible = { always = no }

		days_remove = project_construction_duration^5

		cancel_trigger = { NOT = { check_variable = { ROOT.internal_project_array^5 < 0 } } }

		remove_effect = {
			log = "[GetDateText]: [ROOT.GetName]: Decision investments_project_5_decision"
			set_temp_variable = { completed_project_building_type = project_building_type^5 }
			custom_effect_tooltip = {
				localization_key = internal_investment_completion_tooltip_tt
				BUILDING = [investments_get_completed_building_type]
				AMOUNT = [?ROOT.project_build_amount^5]
			}
			hidden_effect = {
				set_variable = { project = 5 }
				country_event = investments_event.0
			}
		}
	}
}
